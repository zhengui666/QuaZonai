import { Button } from '@radix-ui/themes';import type { ColumnDef } from '@tanstack/react-table';import { DataTable } from '../components/ui/DataTable';import { ErrorPanel } from '../components/ui/ErrorPanel';import { PageHeader } from '../components/ui/PageHeader';import { PageSkeleton } from '../components/ui/Skeleton';import { StateBadge } from '../components/ui/StateBadge';import { useHandoffs,useRevokeHandoff } from '../lib/api/hooks';import type { HandoffOffer } from '../lib/api/types';import { formatDateTime } from '../lib/format';function Revoke({offer}:{offer:HandoffOffer}){const m=useRevokeHandoff(offer.id);const revocable=['APPROVED','PUBLISHING','AVAILABLE'].includes(offer.state);return revocable?<Button size="1" variant="soft" color="red" disabled={m.isPending} onClick={()=>m.mutate('OPERATOR_REVOKE')}>{m.isPending?'Revoking…':'Revoke offer'}</Button>:<span className="qz-section-meta">{['CLAIMED','DOWNSTREAM_ACCEPTED','FEEDBACK_PENDING','FEEDBACK_RECEIVED'].includes(offer.state)?'Downstream owns runtime':'Historical'}</span>}const cols:ColumnDef<HandoffOffer,unknown>[]=[{accessorKey:'downstream_name',header:'Downstream',cell:({row})=><div><div className="qz-list-title">{row.original.downstream_name??row.original.downstream_system_id?.slice(0,8)??'—'}</div><div className="qz-list-subtitle">{row.original.purpose??'—'} · Candidate {row.original.candidate_id?.slice(0,8)??'—'}</div></div>},{accessorKey:'state',header:'Offer state',cell:({getValue})=><StateBadge state={String(getValue())}/>},{accessorKey:'feedback_state',header:'Feedback',cell:({getValue})=><StateBadge state={String(getValue()??'PENDING')}/>},{accessorKey:'claim_deadline',header:'Claim deadline',cell:({getValue})=>formatDateTime(getValue() as string|null)},{accessorKey:'package_contract_version',header:'Package contract'},{accessorKey:'feedback_contract_version',header:'Feedback contract'},{id:'action',header:'',cell:({row})=><Revoke offer={row.original}/>,enableSorting:false}];export function HandoffFeedbackPage(){const q=useHandoffs();if(q.isLoading)return<PageSkeleton/>;if(q.error)return<ErrorPanel error={q.error}/>;return <><PageHeader title="Handoff & Feedback" description="QuaZonai publishes immutable Candidate Packages and consumes structured forward evidence. Once a downstream claims an offer, this workbench deliberately has no stop, undeploy or close-position control."/><DataTable data={q.data??[]} columns={cols} searchPlaceholder="Filter handoffs…" emptyTitle="No handoffs" emptyDescription="Approved candidates appear here after their Candidate Package is published." getRowId={r=>r.id}/></>}
