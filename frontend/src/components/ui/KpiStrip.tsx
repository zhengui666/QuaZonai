import type{ReactNode}from'react';export interface KpiItem{label:string;value:ReactNode;note?:ReactNode}export function KpiStrip({items}:{items:KpiItem[]}){return<div className="qz-kpi-strip">{items.map(item=><div className="qz-kpi" key={item.label}><div className="qz-kpi-label">{item.label}</div><div className="qz-kpi-value qz-number">{item.value}</div>{item.note?<div className="qz-kpi-note">{item.note}</div>:null}</div>)}</div>}
