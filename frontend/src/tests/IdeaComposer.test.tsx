import { fireEvent,screen,waitFor } from '@testing-library/react';import { beforeEach,describe,expect,it,vi } from 'vitest';import { IdeaComposerPage } from '../pages/IdeaComposerPage';import { jsonResponse,renderApp } from './testUtils';describe('IdeaComposer',()=>{beforeEach(()=>vi.restoreAllMocks());it('previews the frozen charter and starts research',async()=>{const fetchMock=vi.spyOn(globalThis,'fetch').mockImplementation((input)=>{const url=String(input);if(url.endsWith('/ideas/preview'))return jsonResponse({charter:{original_idea_text:'idea',research_question:'Does drift persist?',prediction_horizon:'1D',market_scope:'US Equities'},clarification_required:false,overlap:null});if(url.endsWith('/research-programs'))return jsonResponse({id:'p-1',state:'ACTIVE'});return jsonResponse({},404)});renderApp(<IdeaComposerPage/>,{route:'/ideas'});fireEvent.change(screen.getByPlaceholderText(/Test whether short-horizon/i),{target:{value:'Study post earnings drift in liquid US equities for one day.'}});fireEvent.click(screen.getByRole('button',{name:/Preview research charter/i}));expect(await screen.findByText('Does drift persist?')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Start Research'}));await waitFor(()=>expect(fetchMock).toHaveBeenCalledWith('/api/v1/research-programs',expect.objectContaining({method:'POST'})))})});
