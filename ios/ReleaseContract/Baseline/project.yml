name: QuaZonaiBaseline
options:
  deploymentTarget:
    iOS: 18.0
settings:
  base:
    SWIFT_VERSION: 6.0
    TARGETED_DEVICE_FAMILY: "1,2"
targets:
  QuaZonaiBaseline:
    type: framework
    platform: iOS
    deploymentTarget: 18.0
    sources:
      - path: ../../ReleaseContractEvidence.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.quazonai.operator.baseline
        SKIP_INSTALL: YES
