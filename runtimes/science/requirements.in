# Direct W0 dependencies; requirements.lock contains the complete resolved graph.
# Update with uv pip compile against Python 3.12, then rerun the native probes.
nautilus-trader==1.231.0
# GitHub v1.0.4 is not on PyPI; verify the actually published wheel.
# CPCV is NOT accepted by this probe and requires separate native purge tests.
skfolio==1.0.3
pyarrow==25.0.1
optuna==4.9.0
