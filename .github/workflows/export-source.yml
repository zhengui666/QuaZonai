name: Export source snapshot

on:
  push:
    branches:
      - feat/issue-22-remote-nautilus-runtime

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: quazonai-source
          path: .
          include-hidden-files: true
          retention-days: 1
